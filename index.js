// Write your code here!
// Remove main using the ID 
document.getElementById('main').remove();

// Create a new header variable that points to h1.
const newHeader = document.createElement('h1');

// Assign the id "victory" to the new header.
newHeader.id = 'victory';

// Assign text to the new header variable.
newHeader.innerHTML = "TIM-BAROS is the champion";


